using System;
using System.IO;
using System.Reflection;

namespace XamQuiz.Helpers
{
    public static class LocalJson
    {
    
        //private void LocalJson()
        //{
        //    var assembly = typeof(WODManagementView).GetTypeInfo().Assembly;
        //    Stream stream = assembly.GetManifestResourceStream("MyNamespace.Filename.json");
        //    //this code can't read json file.. stream has null value
        //    using (StreamReader sr = new StreamReader(stream))
        //    {
        //        using (JsonReader reader = new JsonTextReader(sr))
        //        {
        //            JsonSerializer serializer = new JsonSerializer();
        //            JArray jarray = serializer.Deserialize(reader) as JArray;
        //        }
        //    }
        //}
    }
}
